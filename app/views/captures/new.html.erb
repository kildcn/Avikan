<h1>Upload a picture</h1>

<%# The form comes here %>

<%= simple_form_for(@new_capture)do |f| %>
  <%= f.input :image, as: :file %>
  <%= f.button :submit %>
<% end %>

<p>Here comes the bird hash</p>
<%= @bird_hash.first_likely_bird_species%>