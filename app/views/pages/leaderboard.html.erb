<h1>Global Leaderboard</h1>


<div class="leaderboard-container">
  <ol>
    <% @global_top_3.each_with_index do |user, index| %>
    <li><%= link_to "#{user.username} -  #{user.user_xp} xp", user_badges_path(user) %></li>
    <% end %>
  </ol>
</div>
